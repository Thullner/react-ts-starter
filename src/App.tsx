import React, { FunctionComponent } from 'react';

interface OwnProps {}

type Props = OwnProps;

const App: FunctionComponent<Props> = (props) => {

  return (
    <div>
      Test2
    </div>
  );
};

export default App;
